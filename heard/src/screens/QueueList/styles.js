import { StyleSheet, StatusBar } from 'react-native';


const styles = StyleSheet.create({
    container: {
        flex: 1,
        marginTop: 0,
    },
    textInput:{
        marginBottom: 30,
        marginTop: 10
    }
});

export  default styles;